# Nattlig gjennomgang & bygg — 2026-08-21

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som hver natt siden 2026-08-02: den lagrede trigger-teksten
ber om en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner»,
bygg minst 3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview,
aldri `main`). `CLAUDE.md` (sist oppdatert 2026-07-18, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt, som hver natt siden 08-02. Trigger-teksten er fortsatt ikke
oppdatert til å matche — rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt.
`idea-backlog.md` er uendret — ingenting nytt utenfor de tre tillatte
kategoriene dukket naturlig opp under gjennomgangen i natt.

## Sammendrag

Baseline (`main`, commit `8e2a946`) verifisert grønn: ren `npm ci` +
`npm run build` uten feil, helt ren `node_modules`/`.next`. `npm audit`:
samme 2 kjente høye sårbarheter (next/postcss) som forrige netter —
uendret, siden `nightly/2026-08-18/nextjs-16-upgrade` fortsatt venter på
din nettleser-sjekk (se under).

Tre parallelle underagenter (sikkerhet, ytelse, backoffice) gjorde hver sin
uavhengige, dype gjennomgang av hele diffen siden forrige dype gjennomgang
(261d8f9, 2026-08-15) pluss et friskt sveip av områder ikke eksplisitt
nevnt i tidligere rapporter, orientert om siste netters funn for å unngå
duplikater:

- **Sikkerhet**: ingen nye funn. Sjuende natt på rad ren. Full diff siden
  261d8f9 lest (`lib/supabaseRest.js`, `lib/geoPrivacy.js`, `lib/nvdb.js`,
  `lib/emailTransport.js`, `lib/dailySummaryEmail.js`, `pages/api/report.js`,
  den nye `pages/api/backoffice/statistikk.js`,
  `pages/api/backoffice/attachment.js`,
  `pages/api/backoffice/ai/report.js`, `lib/backofficeAi.js`,
  `components/ReportMap.js`, `components/TripTracker.js`,
  `lib/mapStyleConfig.js` m.fl.), pluss friske sjekker: ingen
  `middleware.js`, `next.config.js`/`vercel.json` uendret bortsett fra
  kjent CSP-gap, ingen CORS-håndtering, `lib/staffAuth.js`-cookien fortsatt
  `HttpOnly; Secure; SameSite=Lax`, ingen `process.env`-lekkasje til
  klientbunten (kun `NEXT_PUBLIC_*` i klientkode), den nye
  vedlegg-bøtte-flytt-logikken fortsatt bak `isAdminRequest` før enhver
  gren. Nytt `trip_start_lat/lng`-felt i `POST /api/report` brukes kun
  defensivt (serveren regner uansett ut sitt eget forskjøvede
  personvern-ankerpunkt, stoler aldri på klientens verdi som "hjem").
  `npm audit --production`: samme 2 kjente sårbarheter, ingen nye.
- **Ytelse**: ingen nye SAFE-MECHANICAL- eller høy/middels-funn. To nye,
  lave NEEDS-JUDGMENT-observasjoner (ikke byggeklare, krever et bevisst
  cache-valg, se anbefaling nr. 16 og 17 under): (1) `listCaseAttachments()`
  signerer nå én fersk URL per internt vedlegg per saksvisning (korrekt
  følge av 08-18-sikkerhetsfiksen, parallellisert, neglisjerbart ved
  dagens skala, men en reell fremtidig cache-mulighet); (2) den nye
  `/api/backoffice/statistikk` regner om på hver forespørsel/range-bytte
  uten cache (bundet til 2000 rader, admin-only, lav trafikk — ikke et
  problem i dag). Ellers alt sjekket rent: ulykkes-varmekart-laget
  introduserer ingen nye nettverkskall (gjenbruker eksisterende
  bbox/zoom-gate), NVDB-paginering-tidsbudsjettet gjenbrukes korrekt,
  `package.json` uendret, vedleggsgrenser (10 MB/fil, 12 filer) uendret,
  `GET /api/reports`-taket (5000) og `listReportsForStats` begge korrekt
  indeksert/begrenset. Ingen motsigelse av de to allerede åpne
  NEEDS-JUDGMENT-punktene (`/api/reports`-skalering, `waitUntil`-e-post).
- **Backoffice**: ett nytt funn, **Medium / SAFE-MECHANICAL**, bygget (se
  tabell under): `/backoffice/tetthet.js` sitt `load()` nullstilte aldri
  `stats` før en ny henting og hadde ingen vern mot svar som kommer i feil
  rekkefølge. Ved bytte av konkurranse eller modus-filter kunne forrige
  konkurranses spor/leaderboard bli stående synlig sammen med en
  feilmelding for den nye (misvisende — ser ut som data for den nye
  konkurransen), eller et sent svar kunne overskrive et nyere valgs data.
  Samme `cancelled`/request-id-mønster som `statistikk.js` allerede
  bruker. Alle andre backoffice-sider (`hotteste`, `gjentakende-steder`,
  `liste`, `statistikk`, `sak/[id]`, `skilt/[id]`, `konkurranser`,
  `seed-spor`) gjennomgått på nytt — feil-/tom-/laster-tilstand
  fortsatt gjensidig utelukkende overalt (mønsteret fra 08-20-fiksen
  holder), dobbel-innsending-vern og slette-bekreftelser fortsatt på
  plass, ingen nye funn.

1 mekanisk fiks er bygget på egen branch, build-verifisert grønt, **merget
til `main`** (ren merge, ingen konflikter), og `main` er build-verifisert
grønt på nytt (helt ren `npm ci` + `.next`, ingen node_modules-cache) etter
merge, før push.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for denne — den er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🔵 Backoffice | `/backoffice/tetthet` («Sykkelspor») viste av og til forrige konkurranses spor/leaderboard sammen med en feilmelding ved bytte av konkurranse/filter, eller lot et sent svar overskrive et nyere valg — `load()` nullstiller nå `stats` før hver henting og har et request-id-vern mot svar i feil rekkefølge | `nightly/2026-08-21/tetthet-stale-state-race` |

Kombinert `main` bygget grønt (`npm ci && npm run build` med helt ren
`node_modules`/`.next`, ingen cache) rett før push. `npm audit` uendret på
2 høye sårbarheter (next/postcss, se punkt 11 i anbefalingslisten under).

## To branches venter fortsatt på din gjennomsjekk i nettleser (uendret siden 08-18)

Begge ble bygget natten til 08-18, verifisert grønt, men **ikke merget**
fordi de kan endre synlig kart-/font-oppførsel på måter som bør sjekkes i
en ekte nettleser først. Ingen av dem er rørt i natt.

- **CSP** (`nightly/2026-08-18/content-security-policy`) — preview:
  `https://finns-vei-git-nightly-2026-08-b306c6-fredrik-jacobsens-projects.vercel.app`.
  Sjekk kart (panorer/zoom/lag), kart-thumbnails, geocoding, fonter
  (Fraunces/Mulish), og konsollen for CSP-varsler.
- **Next.js 16-oppgradering** (`nightly/2026-08-18/nextjs-16-upgrade`,
  next 14.2.35→16.3.1, react 18→19) — preview:
  `https://finns-vei-git-nightly-2026-08-b66cb2-fredrik-jacobsens-projects.vercel.app`.
  Lukker de 2 gjenværende `npm audit`-sårbarhetene helt (0 sårbarheter på
  branchen). Sjekk Mapbox WebGL-rendering, touch/gest, fonter, QR-kode,
  OG-bilde, full backoffice-innlogging.

(Merk: begge branchene er skapt før flere netters `main`-endringer siden
08-18 — om du merger en av dem senere, sjekk at `main`-endringene siden er
tatt med, f.eks. via en rebase/merge av `main` inn i branchen først.)

## Anbefaling

Videreført fra 08-20, med to nye lave punkter lagt til (16, 17):

1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, bekreftet fortsatt uendret i natt).
2. **Manglende Content-Security-Policy** — branch klar, venter på din
   nettleser-sjekk (se over).
6. **`listHotCases`-rangeringsskjevheten**, **konkurranse-redigering-UI**,
   **bruker-redigering-UI**, **seed-spor-nav-synlighet** — alle sover
   fortsatt, ingen hastverk.
8. **Vurder å rydde den ene foreldreløse `case_attachments`-raden** —
   destruktiv, bevisst ikke automatisk.
9. **Trello-kortløs-sak-begrensningen** — fortsatt åpent, venter på deg.
10. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
    produksjon** — kan ikke bekreftes fra denne økten.
11. **Next.js-oppgraderingen** — branch klar, venter på din nettleser-sjekk
    (se over). Lukker `npm audit` helt.
13. Valgfritt opprydningspunkt: de to gamle bildekomprimering-branchene er
    fortsatt overflødige.
14. **`waitUntil`-bakgrunnskjøring for e-postsending i `/api/report`**
    (flagget 08-18) — fortsatt åpent, `@vercel/functions` er fortsatt ikke
    lagt til.
15. **FK-constraint `case_attachments.report_id REFERENCES reports(id) ON
    DELETE CASCADE`** — fortsatt åpent, additiv migrasjon, ingen hastverk.
16. **Cache signerte vedlegg-URL-er** (nytt i natt) — `listCaseAttachments()`
    signerer nå én URL per internt vedlegg per saksvisning. Ikke et
    problem ved dagens skala, men en reell cache-mulighet om
    vedleggsantallet vokser. Krever et bevisst valg om TTL/ferskhet —
    NEEDS-JUDGMENT, ingen hast.
17. **Cache `/api/backoffice/statistikk`** (nytt i natt) — regner om på
    hver forespørsel/range-bytte uten cache. Bundet til 2000 rader,
    admin-only, lav trafikk i dag. Samme NEEDS-JUDGMENT-vurdering som over.

Punkter 3, 4, 5, 7, 12 fra tidligere rapporter er løst og fjernet fra
listen.
