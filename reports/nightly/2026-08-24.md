# Nattlig gjennomgang & bygg — 2026-08-24

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som hver natt siden 2026-08-02: den lagrede trigger-teksten
ber om en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner»,
bygg minst 3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview,
aldri `main`). `CLAUDE.md` (sist oppdatert 2026-07-18, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt, som hver natt siden 08-02. Trigger-teksten er fortsatt ikke
oppdatert til å matche — rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt.
`idea-backlog.md` er uendret — ingenting nytt utenfor de tre tillatte
kategoriene dukket naturlig opp under gjennomgangen i natt (gjennomgangen
i natt var i tillegg eksplisitt scopet kun til de tre kategoriene, så det
var ikke ventet at noe kreativt skulle dukke opp).

## Sammendrag

Baseline (`main`, commit `d272d17`) verifisert grønn: ren `npm ci` +
`npm run build` uten feil, helt ren `node_modules`/`.next`, `npm audit`
0 sårbarheter (uendret siden Next.js 16-oppgraderingen 08-23).

Tre parallelle underagenter (sikkerhet, ytelse, backoffice) gjorde hver sin
uavhengige, dype gjennomgang av hele det aktuelle området, orientert om
forrige natts kjente/åpne punkter for å unngå duplikater:

- **Sikkerhet**: ingen nye funn. **Tiende natt på rad ren.** Frisk,
  uavhengig sjekk av `lib/staffAuth.js`, `lib/backofficeAuth.js`,
  `lib/rateLimit.js`/`lib/rateLimitEdge.js`, `lib/geoPrivacy.js` i sin
  helhet, Trello-webhook-HMAC-en, `staff/*`-rutene,
  `pages/api/backoffice/attachment.js`, `report.js`/`report-thread.js`/
  `my-reports-status.js`, `nvdb/layer.js`, `weather.js`/`aktuelt.js`, CSP i
  `next.config.js`, og samtlige `dangerouslySetInnerHTML`-kall i
  `components/ReportMap.js`. Én liten observasjon uten egen handling: det
  klientstyrte `cells`-arrayet i `POST /api/bike-trips` mangler samme
  eksplisitte lengdetak som `path` fikk 2026-08-23 — vurdert som en
  mindre variant av allerede sovende anbefaling nr. 18 (produktvalg om
  punkttetthet), ikke et nytt, selvstendig funn.
- **Ytelse**: ett tilsynelatende nytt funn — manglende indeks på
  `case_attachments.report_id` — viste seg ved direkte sjekk mot
  produksjonsdatabasen (Supabase) å **allerede finnes**
  (`case_attachments_report_idx`, satt opp tidligere men aldri
  dokumentert). Reelt funn var dermed en dokumentasjonshull, ikke en
  ytelsesrisiko — rettet som en ren dok-fiks (se tabell under), ingen
  skjemaendring. Alt annet ble bekreftet uendret/friskt fra tidligere
  netters herding (batchede `IN`-oppslag, `Promise.all`, TTL-cacher,
  radtak, CDN-cache-headere, nativ Mapbox GL-klustring).
- **Backoffice**: ett nytt funn, bygget i natt (se tabell under): samme
  React-batch-mønster som ble fikset i `submit()` 2026-08-23 fantes
  fortsatt i søsterfunksjonen `toggleActive()` i
  `/backoffice/konkurranser` — en mislykket status-veksling (aktiver/skjul
  konkurranse) satte en feilmelding som umiddelbart ble overskrevet av
  `load()`s egen statusnullstilling i samme render-batch, slik at en
  stab-bruker ikke fikk noen indikasjon på at handlingen feilet. Alle
  andre backoffice-sider og -API-ruter ble gjennomgått på nytt spesifikt
  for samme mønster — ingen flere forekomster funnet.

2 fikser er bygget på egne branches, hver build-verifisert grønt, **merget
til `main`** (rene merger, ingen konflikter), og kombinert `main` er
build-verifisert grønt på nytt (helt ren `npm ci` + `.next`, ingen
node_modules-cache, 0 `npm audit`-sårbarheter) etter begge merger, før
push.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for disse — de er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🔵 Backoffice | `/backoffice/konkurranser` sin `toggleActive()` viste aldri feilmelding ved mislykket aktiver/skjul-veksling (samme stale-state-mønster som `submit()`-fiksen 08-23, i en søsterfunksjon som ikke ble oppdatert da) — `load()`-kallet i catch-leddet ber nå eksplisitt om `resetStatus: false` | `nightly/2026-08-24/konkurranser-toggle-status-message` |
| 2 | 🟢 Ytelse (dok) | `README.md`s indeksliste manglet `case_attachments_report_idx` — indeksen finnes allerede i produksjon, bare udokumentert. Ingen skjemaendring, kun dok-fiks for å unngå at fremtidige gjennomganger flagger den som manglende på nytt | `nightly/2026-08-24/case-attachments-index-doc` |

Kombinert `main` bygget grønt (`npm ci && npm run build` med helt ren
`node_modules`/`.next`, ingen cache) rett før push. `npm audit`: 0
sårbarheter, uendret.

## Anbefaling

Videreført fra 08-23 (punkt 2, 11 og 18 fra 08-23 er allerede fjernet der,
etter kveldens oppfølging samme natt). Ingen nye punkter i natt utover de
to byggede fiksene over.

1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, bekreftet fortsatt uendret i natt).
2. **`listHotCases`-rangeringsskjevheten**, **konkurranse-redigering-UI**,
   **bruker-redigering-UI**, **seed-spor-nav-synlighet** — alle sover
   fortsatt, ingen hastverk.
3. **Vurder å rydde den ene foreldreløse `case_attachments`-raden** —
   destruktiv, bevisst ikke automatisk.
4. **Trello-kortløs-sak-begrensningen** — fortsatt åpent, venter på deg.
5. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
   produksjon** — kan ikke bekreftes fra denne økten.
6. Valgfritt opprydningspunkt: de to gamle bildekomprimering-branchene er
   fortsatt overflødige.
7. **`waitUntil`-bakgrunnskjøring for e-postsending i `/api/report`**
   (flagget 08-18) — fortsatt åpent, `@vercel/functions` er fortsatt ikke
   lagt til.
8. **FK-constraint `case_attachments.report_id REFERENCES reports(id) ON
   DELETE CASCADE`** — fortsatt åpent, additiv migrasjon, ingen hastverk.
9. **Cache signerte vedlegg-URL-er** — `listCaseAttachments()` signerer
   én URL per internt vedlegg per saksvisning. Ikke et problem ved
   dagens skala. NEEDS-JUDGMENT, ingen hast.
10. **Cache `/api/backoffice/statistikk`** — regner om på hver
    forespørsel/range-bytte uten cache. Admin-only, lav trafikk i dag.
    Samme NEEDS-JUDGMENT-vurdering som over.
11. **Eksplisitt lengdetak på `cells`-arrayet i `POST /api/bike-trips`**
    (ny observasjon i natt, sikkerhetsagenten) — mindre variant av punkt
    18 fra 08-23 (GPS-punkttak), samme produktvalg om punkttetthet.
    NEEDS-JUDGMENT, ingen hast.

Ni netter på rad uten nye sikkerhetsfunn og et stadig kortere funnvindu
generelt (i natt: 2 småfikser, hvorav den ene viste seg å være en
dokumentasjonsrettelse snarere enn en reell endring) tyder på at
sikkerhets-/ytelses-/backoffice-flaten i dette repoet nå er godt herdet —
fremtidige netter vil trolig i økende grad bekrefte «ingen nye funn»
snarere enn å finne nytt.
