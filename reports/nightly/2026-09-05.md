# Nattlig gjennomgang & bygg — 2026-09-05

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som hver natt siden 2026-08-02: den lagrede trigger-teksten
ber om en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner»,
bygg minst 3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview,
aldri `main`). `CLAUDE.md` (sist oppdatert 2026-07-18, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt, som hver natt siden 08-02. Trigger-teksten er fortsatt ikke
oppdatert til å matche — rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt. Ingen nye
produktforslag dukket opp under gjennomgangen som ikke allerede står i
`idea-backlog.md` — den er derfor ikke endret i natt.

## Sammendrag

Baseline (`main`, commit `3261dd7`) verifisert grønn før noe ble bygget: ren
`npm ci` + `npm run build` uten feil, 0 sårbarheter.

Tre parallelle underagenter (sikkerhet, ytelse, backoffice) gjorde hver sin
uavhengige, dype gjennomgang av hele det aktuelle området, orientert om
rapportene fra 2026-09-01 til 2026-09-04 for å unngå duplikater. Ingen kode
hadde endret seg siden 09-04-mergen (HEAD var nøyaktig den mergen), så det
var ingen ny diff å regresjonssjekke utover det som ble sjekket i natt.

**Sikkerhet: ingen nye funn (35. natt på rad).** Gjennomgikk på nytt alle
`pages/api/**/*.js`-ruter med spesielt fokus på det som ikke eksplisitt var
navngitt "bekreftet rent" tidligere: `debug/report.js`, `debug/enrich.js`
(hemmelighets-gating + rate-limiting), alle fire
`backoffice/ai/*`-endepunktene (auth via `isAdminRequest`, rate-limits,
IDOR-sjekker), `staff/bootstrap.js` (engangs-superbruker-oppretting),
OG-bilde-rutene (bekreftet 09-04-fiksen fortsatt korrekt, ingen
injeksjonsvei i Satori-rendring), `lib/rateLimit.js`, `lib/staffAuth.js`
(cookie/sesjon, `SameSite=Lax`), `lib/timingSafeEqualStrings.js` og Trello-
webhookens signaturverifisering. `npm audit`: 0 sårbarheter.

**Ytelse: ingen nye funn (35. natt på rad).** Gjennomgikk funksjoner i
`lib/supabaseRest.js` ikke tidligere navngitt enkeltvis
(`getPublicReportsByIds`, `getCompetitionStats`, `getReportsSince`),
`my-reports-status`, `competitions/[id]`, cron-digesten,
backoffice-statistikk/hot-cases/hotspots-cachingen, samt klientsidene
`mine-meldinger.js`/`mine-turer.js` og `ReportMap.js`s fetch-punkter. Alt
allerede begrenset/cachet/parallellisert av tidligere netter.
Regresjonssjekk: `REPORT_THREAD_SUPPORT_LIMIT` (09-03) og
`REPORT_THREAD_UPDATES_LIMIT` (09-04) begge fortsatt korrekt til stede og
riktig sortert i `getReportThread()`.

**Backoffice: 1 SAFE-MECHANICAL-funn (bygget og merget).** Gjennomgikk hele
`pages/backoffice/sak/[id].js` på nytt, alle fire `backoffice/ai/*`-
endepunktene (bakgrunn for punkt 13 under), `konkurranser.js` og
`brukere.js` i sin helhet.

1. **[SAFE-MECHANICAL, bygget]** `deleteCase` i
   `pages/backoffice/sak/[id].js` var den ene gjenværende
   state-muterende handleren i filen uten `activeIdRef`-vakten som
   `changeStatus`/`changeAssignee`/`changeDue` (09-03) og
   `addNote`/`doUpload`/`toggleAtt`/`deleteAtt` (09-04) alt har. Staben
   arm­er slett-dialogen, skriver SLETT, klikker "Slett saken permanent"
   (fokus havner på selve knappen, ikke et input-felt, så
   piltast-sak-navigasjonen fortsatt virker), og navigerer raskt til en
   annen sak mens slette-forespørselen for den forrige fortsatt er i
   flukt. Ved suksess sendte den forsinkede responsen staben tilbake til
   sakslisten fra saken de nå faktisk står på (ikke saken som ble
   slettet); ved feil viste den en misvisende feil-flash på den nye saken.
   Selve slettingen skjedde uansett riktig server-side — dette var kun en
   navigasjons-/meldings-bug, ikke datatap. Fikset ved å legge til nøyaktig
   samme `activeIdRef.current === id`-vakt-mønster som resten av filen,
   både på suksess- og feil-stien.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for denne — den er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🔵 Backoffice | Utvid sak-kappløpsvakten (`activeIdRef`) til `deleteCase` — siste gjenværende handler uten vakten | `nightly/2026-09-05/sak-delete-race-guard` |

Branchen ble bygget og build-verifisert (`npm ci` + `npm run build`)
enkeltvis først, deretter merget inn i `main` (ren merge, ingen konflikt).
Kombinert `main` ble bygget grønt på nytt (`npm ci && npm run build`, helt
ren `node_modules`/`.next`, ingen cache) rett før push. `npm audit`: 0
sårbarheter, både før og etter. `main` er nå på `bd2c76f`.

## Ingenting bygget kun som preview i natt

Ingen nye NEEDS-JUDGMENT-funn i natt. Det eneste funnet (backoffice,
`deleteCase`) var entydig SAFE-MECHANICAL og er allerede merget.

## Sjekk dette (for den ene som er live)

1. **`deleteCase`-kappløpsfiksen**: gå til en sak i backoffice, arm
   slette-dialogen (skriv SLETT), klikk "Slett saken permanent", naviger
   raskt til en annen sak (piltast) før forespørselen er ferdig, og
   bekreft at du blir værende på den nye saken (ingen uventet
   redirect/feil-flash for en handling du ikke utførte der). Samme
   vanskelig-å-reprodusere-pålitelig-uten-kunstig-forsinkelse-forbehold
   som 09-03/09-04; koden er build-verifisert og følger et allerede
   etablert mønster i samme fil.

## Anbefaling

Videreført fra 09-04, uendret (ingen av de 15 punktene ble adressert i
natt — kveldens ene funn var nytt og er allerede løst og fjernet fra
listen):

1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, fortsatt uendret).
2. **Avgjør `waitUntil`-branchen fra 08-25**
   (`nightly/2026-08-25/report-email-waituntil`) — bygget og
   build-verifisert, venter fortsatt på din vurdering i preview før
   eventuell merge.
3. `listHotCases`-rangeringsskjevheten, konkurranse-redigering-UI,
   bruker-redigering-UI, seed-spor-nav-synlighet — alle sover fortsatt.
4. **Rydd den ene foreldreløse `case_attachments`-raden** — destruktiv,
   bevisst ikke automatisk.
5. **Trello-kortløs-sak-begrensningen** — fortsatt åpent, venter på deg.
6. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
   produksjon** — kan ikke bekreftes fra denne økten. Fortsatt det
   lengst-stående uavklarte punktet på listen (siden 08-02, nå over en
   måned).
7. Valgfritt opprydningspunkt: de to gamle bildekomprimering-branchene er
   fortsatt overflødige.
8. **FK-constraint `case_attachments.report_id REFERENCES reports(id) ON
   DELETE CASCADE`** — fortsatt åpent, additiv migrasjon, ingen hastverk.
9. **Cache signerte vedlegg-URL-er** — fortsatt NEEDS-JUDGMENT, ingen hast.
10. **Konsolider `distMeters`/haversine-duplikater** — fortsatt
    NEEDS-JUDGMENT.
11. `cells`-array-lengdetak i `POST /api/bike-trips` — bekreftet 08-25 at
    det ikke er et reelt problem, kan lukkes permanent med mindre du vil ha
    det av ren symmetri med `path`.
12. Signert-URL-lekkasje-monitor for interne vedlegg (rent produktforslag
    fra 08-28, se `idea-backlog.md`) — kun aktuelt om/når omfanget utvides
    tilbake til produktideer.
13. **Backoffice-UI for de eksisterende AI-forslag-endepunktene**
    (`/api/backoffice/ai/*`) — fortsatt uten UI som kaller dem, fortsatt et
    reelt produktvalg — NEEDS-JUDGMENT, ikke bygget.
14. **Trello-webhook uten replay-beskyttelse** (flagget 09-03) — lav
    praktisk alvorlighet (idempotent/spam-nivå, krever allerede en gyldig
    signert payload), men krever et lagrings-/TTL-designvalg for dedup på
    `action.id` før den kan bygges.
15. **Batchede support/status-spørringer uten per-sak-cap** i
    `getPublicReportGeoJson`/`listHotCases` (flagget 09-04) — lav/
    spekulativ alvorlighet i dag, men krever et spørrings-form- eller
    cap-valg, ikke en mekanisk patch.

35 netter uten nye sikkerhetsfunn av betydning og et fortsatt tynt
ytelses-vindu bekrefter at flaten er svært godt herdet innenfor det
gjeldende omfanget. Kveldens eneste funn (én siste handler i
`sak/[id].js` som ikke fikk kappløpsvakten i de to foregående nettenes
opprydning) tyder på at den runden nå er fullstendig — samtlige
state-muterende handlere i filen har vakten. Med et stadig tynnere
funn-vindu tre kvelder på rad (1 funn 09-03, 3 funn 09-04, 1 funn i natt,
alle mekaniske og lav/moderat alvorlighet) er vurderingen fra i går
uendret: verdt å vurdere om det er tid for enten (a) et bevisst signal
fra deg om å utvide omfanget tilbake til produktideer, eller (b) en
gjennomgang av de 15 åpne punktene for å avgjøre noen av dem — særlig
punkt 6, som fortsatt er det eneste punktet direkte knyttet til
produksjons-sikkerhetskonfigurasjon jeg ikke kan bekrefte selv, og som nå
har stått uavklart i over en måned.
