# Nattlig gjennomgang & bygg — 2026-09-07

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som hver natt siden 2026-08-02: den lagrede trigger-teksten
ber om en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner»,
bygg minst 3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview,
aldri `main`). `CLAUDE.md` (sist oppdatert 2026-07-18, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt, som hver natt siden 08-02. Trigger-teksten er fortsatt ikke
oppdatert til å matche — rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt. Ingen nye
produktforslag dukket opp under gjennomgangen som ikke allerede står i
`idea-backlog.md` — den er derfor ikke endret i natt.

## Sammendrag

Baseline (`main`, commit `8156ecf`) verifisert grønn før noe ble bygget: ren
`npm ci` + `npm run build` uten feil, 0 sårbarheter.

Tre parallelle underagenter (sikkerhet, ytelse, backoffice) gjorde hver sin
uavhengige, dype gjennomgang av hele det aktuelle området, orientert om
rapportene fra 2026-09-04 til 2026-09-06 for å unngå duplikater og bedt om å
angripe problemstillingen fra vinkler som ikke eksplisitt var navngitt i
tidligere netter (heller enn å bare bekrefte gamle funn på nytt).

**Sikkerhet: ingen nye funn (37. natt på rad).** Frisk gjennomgang fra
vinkler ikke tidligere eksplisitt navngitt: prototype pollution (alle
`JSON.parse`/`Object.assign`-treffpunkt), ReDoS (alle regex-er mot ekstern
input i `lib/feeds.js`, `lib/nvdb.js`, `lib/multipart.js`, Trello-webhook),
SSRF (bekreftet at kun env-konfigurerte hoster brukes til utgående kall,
aldri request-avledede), CSRF (session-cookien er
`HttpOnly; Secure; SameSite=Lax`), IDOR via sekvensielle ID-er (bekreftet
at alle tabeller bruker `uuid`, ingen inkrementerende ID-er), hemmeligheter
i klient-bundlen (kun de to tiltenkt offentlige `NEXT_PUBLIC_*`-variablene
er referert utenfor `pages/api/**`/`lib/*.js`), samt friske stikkprøver av
CSP/HSTS, cron-hemmelighetssjekken og rate-limiting på de nyere/mindre
endepunktene. `npm audit`: 0 sårbarheter.

**Ytelse: ingen nye SAFE-MECHANICAL-funn.** Frisk gjennomgang av filer ikke
tidligere navngitt enkeltvis (alle `pages/api/backoffice/*.js`,
`pages/api/cron/daily-summary.js`, `pages/api/reports.js`,
`pages/api/aktuelt.js`, `pages/api/weather.js`,
`lib/myReports.js`/`lib/myTrips.js`/`lib/budProgress.js`,
`lib/reportWorkflow.js`, `lib/nvdb.js`s `enrichPoint`, `lib/feeds.js`, samt
flere backoffice-klientsider). Alt allerede rate-limitet, cappet eller
memoisert fra tidligere netter. Status på de stående NEEDS-JUDGMENT-punktene
er uendret, med ett avklart: `nightly/2026-08-25/report-email-waituntil`
ble bekreftet via en tørrkjørt merge (`git merge --no-commit --no-ff`, så
avbrutt) å fortsatt merge helt konfliktfritt mot dagens `main` — venter
fortsatt på din vurdering i preview, ingen rebase nødvendig.

**Backoffice: 2 SAFE-MECHANICAL-funn (bygget og merget).** Frisk
gjennomgang av alle sider ikke tidligere navngitt enkeltvis
(`brukere.js`, `konkurranser.js`, `index.js`, `hotteste.js`,
`gjentakende-steder.js`, `tetthet.js`, `statistikk.js`, `seed-spor.js`,
`skilt/[id].js`, `liste.js`), pluss en full re-lesning av `sak/[id].js`
(797 linjer) som bekreftet at kappløpsvakten og flash/kopiert-nullstillingen
fra tidligere netter fortsatt er intakt og fullstendig.

1. **[SAFE-MECHANICAL, bygget]** `pages/backoffice/sak/[id].js`:
   `fetchAccidents()` setter `accidents` til strengen `'error'` når
   NVDB-kallet feiler, men UI-en sjekket kun `Array.isArray(accidents)` —
   en mislykket henting rendret dermed ingenting, umulig å skille fra et
   ekte "0 ulykker i nærheten"-resultat. `styles/app.css` hadde allerede en
   ferdig `.case-admin__accidents-err`-stil definert for nøyaktig dette
   tilfellet, men den ble aldri koblet til i komponenten. Fikset ved å
   legge til den ene manglende render-grenen.
2. **[SAFE-MECHANICAL, bygget]** `pages/backoffice/tetthet.js`: den
   interne "Sykkelspor"-ledertavlen (plass/klubb/turer/km) viste rene tall
   for hver rad, mens `components/CompetitionSheet.js` — den
   borgervendte ledertavlen med nøyaktig samme datastruktur — allerede har
   gull/sølv/bronse-plassmerker og uthevet ledende rad. Ren kosmetisk
   inkonsistens mellom to visninger av samme konkurransedata; portert det
   identiske, allerede utprøvde mønsteret ord for ord. Ingen endring i
   rangeringslogikk.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for disse — de er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🔵 Backoffice | Vis feilmelding når NVDB-ulykkesdata ikke kunne hentes, i stedet for stille tomrom | `nightly/2026-09-07/sak-accidents-error-state` |
| 2 | 🔵 Backoffice | Gull/sølv/bronse-plassmerker + uthevet ledende rad på den interne sykkelspor-ledertavlen (samme mønster som borgervendt ledertavle) | `nightly/2026-09-07/tetthet-leaderboard-rank-styling` |

Hver branch ble bygget og build-verifisert (`npm ci` + `npm run build`)
enkeltvis først av sin underagent, deretter merget inn i `main` her (to
rene merger, ingen konflikt). Kombinert `main` ble bygget grønt på nytt fra
helt ren `node_modules`/`.next` rett før push. `npm audit`: 0 sårbarheter.
`main` er nå på `daedae6`.

## Ingenting bygget kun som preview i natt

Ingen nye NEEDS-JUDGMENT-funn i natt utover de allerede stående punktene
under. Begge kveldens funn var entydig SAFE-MECHANICAL og er allerede
merget.

## Sjekk dette (for de to som er live)

1. **Ulykkesdata-feilmeldingen**: åpne en sak i backoffice
   (`/backoffice/sak/[id]`) der NVDB-ulykkesdata normalt lastes. Vanlig
   tilfelle skal se uendret ut. For å teste selve feilmeldingen trengs en
   simulert NVDB-feil (f.eks. midlertidig feil URL/tidsavbrudd) — ikke
   noe staben trenger å gjøre proaktivt, men om noen tilfeldigvis støter på
   en reell NVDB-nedetid fremover, bør de nå se en tydelig feiltekst i
   stedet for et stille tomrom.
2. **Sykkelspor-ledertavlen**: åpne `/backoffice/tetthet`, se på
   "Sykkelspor"-ledertavlen. Bekreft at rad 1 (om den har turer > 0) nå har
   gyllen plassmerke og uthevet bakgrunn, rad 2/3 sølv/bronse, resten
   uendret. Ingen endring i hvem som rangeres først — kun visuell stil.

## Anbefaling

Videreført fra 09-06, uendret i substans (kveldens to funn var nye og er
allerede løst og fjernet fra listen; ett punkt er nå avklart som fortsatt
konfliktfritt uten at det endrer statusen):

1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, fortsatt uendret).
2. **Avgjør `waitUntil`-branchen fra 08-25**
   (`nightly/2026-08-25/report-email-waituntil`) — bygget,
   build-verifisert, og i natt bekreftet fortsatt konfliktfri mot dagens
   `main`. Venter fortsatt på din vurdering i preview før eventuell merge.
3. `listHotCases`-rangeringsskjevheten, konkurranse-redigering-UI,
   bruker-redigering-UI, seed-spor-nav-synlighet — alle sover fortsatt.
4. **Rydd den ene foreldreløse `case_attachments`-raden** — destruktiv,
   bevisst ikke automatisk.
5. **Trello-kortløs-sak-begrensningen** — fortsatt åpent, venter på deg.
6. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
   produksjon** — kan ikke bekreftes fra denne økten. Fortsatt det
   lengst-stående uavklarte punktet på listen (siden 08-02, nå over en
   måned).
7. Valgfritt opprydningspunkt: de to gamle bildekomprimering-branchene er
   fortsatt overflødige.
8. **FK-constraint `case_attachments.report_id REFERENCES reports(id) ON
   DELETE CASCADE`** — fortsatt åpent, additiv migrasjon, ingen hastverk.
9. **Cache signerte vedlegg-URL-er** — fortsatt NEEDS-JUDGMENT, ingen hast.
10. **Konsolider `distMeters`/haversine-duplikater** — fortsatt
    NEEDS-JUDGMENT. Bekreftet i natt: 5 uavhengige implementasjoner, og
    `sak/[id].js`s versjon har motsatt argumentrekkefølge (lng/lat) av de
    andre — nettopp derfor er dette ikke en mekanisk fiks (lett å innføre
    en lat/lng-bytte-feil ved sammenslåing). `seed-spor.js`s kopi er
    signaturidentisk med `geoPrivacy.js`s og kunne vært en triviell
    enkeltfil-dedupe, men er et manuelt kjørt demo-data-verktøy med null
    reell ytelsespåvirkning — en kodeoppryddingsdetalj, ikke en ytelsesfiks.
11. `cells`-array-lengdetak i `POST /api/bike-trips` — bekreftet 08-25 at
    det ikke er et reelt problem, kan lukkes permanent.
12. Signert-URL-lekkasje-monitor for interne vedlegg (rent produktforslag
    fra 08-28, se `idea-backlog.md`) — kun aktuelt om/når omfanget utvides
    tilbake til produktideer.
13. **Backoffice-UI for de eksisterende AI-forslag-endepunktene**
    (`/api/backoffice/ai/*`) — fortsatt uten UI som kaller dem, fortsatt et
    reelt produktvalg — NEEDS-JUDGMENT, ikke bygget.
14. **Trello-webhook uten replay-beskyttelse** (flagget 09-03) — lav
    praktisk alvorlighet, krever fortsatt et lagrings-/TTL-designvalg for
    dedup på `action.id`.
15. **Batchede support/status-spørringer uten per-sak-cap** i
    `getPublicReportGeoJson`/`listHotCases` (flagget 09-04) — bekreftet i
    natt fortsatt reell men krever et spørrings-form- eller cap-valg, ikke
    en mekanisk patch. (Bekreftet samtidig at `fetchScopedByIds()` allerede
    chunker sitt `IN (...)`-filter, så det er ingen URL-lengde-risiko i dag
    — kun rettferdighets-/tak-spørsmålet fra 09-04 som gjenstår.)
16. **Nytt i natt: `pages/api/backoffice/session.js` ser ut som dødt
    endepunkt.** Ingen klientkall funnet noe sted under
    `pages/backoffice/**` — dagens innloggingsflyt går via `/api/staff/*`
    i stedet. Ser ut som en rest fra en eldre, kun-delt-hemmelighet-basert
    innloggingsdesign. Ikke fjernet i natt — å fjerne et endepunkt er en
    reell atferdsendring (kan påvirke overvåking eller en ekstern kaller vi
    ikke ser), ikke en mekanisk fiks. Verdt et bevisst blikk fra deg.
17. Diverse død CSS (`case-admin__*`, `comp-chip*`, `admin-mode-badge*`,
    `sak-tl*`-familier) fra en lenge fjernet
    `components/CaseAdminPanel.js` — inert opprydningsdetalj uten
    funksjonell effekt, ikke en produktidé, ikke flagget i
    `idea-backlog.md`.

37 netter uten nye sikkerhetsfunn av betydning. Ytelses-vinduet er fortsatt
tynt — ingen nye SAFE-MECHANICAL-funn i natt, kun bekreftelser av at
stående punkter er uendret. Backoffice-sporet ga derimot to nye, konkrete
funn i natt (feilmelding-gap og en ren stil-inkonsistens mellom intern og
ekstern ledertavle) ved å lete i filer som ikke var eksplisitt gjennomgått
tidligere — et tegn på at backoffice fortsatt har litt å hente ved å bevege
seg bredere i kodebasen, i motsetning til sikkerhet/ytelse som nå i økende
grad kun bekrefter tidligere funn. Vurderingen fra i går er uendret: verdt
å vurdere om det er tid for enten (a) et bevisst signal fra deg om å
utvide omfanget tilbake til produktideer, eller (b) en gjennomgang av de nå
17 åpne punktene for å avgjøre noen av dem — særlig punkt 6
(`TRELLO_API_SECRET`/`CRON_SECRET` i produksjon) og det nye punkt 16
(dødt `session.js`-endepunkt), som begge er ting bare du kan bekrefte fra
utsiden av denne økten.
