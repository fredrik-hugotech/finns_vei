# Nattlig gjennomgang & bygg — 2026-08-17

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som de siste ~16 nettene: den lagrede trigger-teksten ber om
en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner», bygg minst
3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview, aldri
`main`). `CLAUDE.md` (sist oppdatert 2026-07-18 av deg, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt. Trigger-teksten er fortsatt ikke oppdatert til å matche —
anbefalingen står fra før: rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt.
`idea-backlog.md` er uendret siden sist — ingenting nytt utenfor de tre
tillatte kategoriene dukket naturlig opp under gjennomgangen i natt.

Merk: det finnes ingen rapport for 2026-08-16 og ingen `nightly/2026-08-16/*`
-branches — rutinen ser ut til ikke å ha kjørt den natten (trigger-gap på
plattformnivå, ikke noe denne økten kan se eller påvirke). Ingen handling
tatt for å kompensere for det — bare notert i tilfelle det er uventet.

## Sammendrag

Baseline (`main`, commit `1856922`) verifisert grønn: ren `npm ci` + `npm run
build` uten feil. `npm audit`: 2 høye sårbarheter (samme kjente
next/postcss-par som forrige natt, ingen nye).

Tre parallelle underagenter (sikkerhet, ytelse, backoffice), hver orientert
om de to siste nettenes funn (08-14, 08-15) for å unngå duplikater, gjorde en
dyp gjennomgang:

- **Sikkerhet**: ingen nye funn — tredje natt på rad. Uavhengig
  re-gjennomgang av staff-auth/sesjon, rate-limiting/IP-utledning,
  Trello-webhook-HMAC, filopplasting (magic-byte-sjekk, UUID-validering,
  størrelsesgrenser), alle ~40 PostgREST-filterbygging-kallsteder i
  `lib/supabaseRest.js`, XSS-escaping på alle `dangerouslySetInnerHTML`, og
  staff-/debug-endepunktene — alt bekreftet solid.
- **Ytelse**: 3 nye funn (alle SAFE-MECHANICAL), bygget. Samme
  over-fetch-mønster (`getReportById` uten eksplisitt `select`) som er
  fikset flere ganger 08-13/08-14/08-15 på andre kallsteder, denne gangen
  funnet i backoffice-AI-forslagsfunksjonen som ikke var gjennomgått
  end-to-end før. Det kjente NEEDS-JUDGMENT-funnet (`GET /api/reports`,
  ubegrenset historikk) er bekreftet fortsatt uendret/uløst, ikke re-listet
  som nytt.
- **Backoffice**: 2 nye funn (begge SAFE-MECHANICAL), bygget.

5 mekaniske fikser er bygget hver på egen branch, build-verifisert grønt,
**merget til `main`** (5 rene merges, ingen konflikter), og `main` er
build-verifisert grønt på nytt (helt ren `npm ci` + `.next`, ingen
node_modules-cache) etter siste merge, før push.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for disse — de er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon. Listet i den
rekkefølgen de ble merget.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🟢 Ytelse | `generateBackofficeAiSuggestion()` (kalt av «AI-forslag»-knappen på hver sak) hentet hele rapportraden (`select=*`, ~35 kolonner inkl. kontaktinfo) for å bygge OpenAI-prompten, men bruker kun 14 navngitte felt pluss `trello_card_id`. Lagt til eksplisitt select | `nightly/2026-08-17/backoffice-ai-suggest-select` |
| 2 | 🟢 Ytelse | `approveReportAiPublicStatus()` hentet hele rapportraden men leser kun `ai_public_status_suggestion` før den oppdaterer raden. Innsnevret til `id,ai_public_status_suggestion` | `nightly/2026-08-17/approve-public-status-select` |
| 3 | 🟢 Ytelse | `GET /api/backoffice/ai/report` hentet hele rapportraden og sendte den umiddelbart gjennom `sanitizeReportForBackofficeAi()`, som kun leser 12 navngitte felt. Lagt til matchende eksplisitt select | `nightly/2026-08-17/ai-report-endpoint-select` |
| 4 | 🔵 Backoffice | `seed-spor.js` sin demo-spor-generator økte suksesstelleren for enhver `POST /api/bike-trips` som ikke kastet unntak, uten å sjekke `r.ok`. Endepunktet rate-limiter til 15 forespørsler/10 min, og verktøyet sender typisk ~40 forespørsler i samme vindu — resten kom tilbake som `429` og ble likevel talt som suksess, så sluttmeldingen «Ferdig: N ruter lagt inn» var feil på akkurat den rate-limiten verktøyet er garantert å treffe. Fikset til å telle etter `r.ok` | `nightly/2026-08-17/seed-spor-success-count` |
| 5 | 🔵 Backoffice | `tetthet.js` og `konkurranser.js` viste faste, generiske feiltekster uansett årsak, ulikt `liste.js`/`hotteste.js`/`gjentakende-steder.js` sitt `describeFetchError`-mønster (viser serverens faktiske feiltekst + Retry-After-hint på 429). Begge sidene bruker nå samme helper | `nightly/2026-08-17/tetthet-konkurranser-error-text` |

Kombinert `main` bygget grønt (`npm ci && npm run build` med helt ren
`node_modules`/`.next`, ingen cache) rett før push, etter merge nr. 5.
`npm audit` uendret på 2 høye sårbarheter (next/postcss, se punkt 11 i
anbefalingslisten under).

## Etterarbeid i natt: opprydding i akkumulert branch-backlog (live sesjon med Fredrik)

Etter den automatiserte rutinen over ba Fredrik om en topp-liste over alt som
har hopet seg opp av `nightly/*`-branches gjennom de siste ~6 ukene, siden han
ikke lenger visste hvor han skulle begynne for å få noe av det ut. En
underagent kartla alle 32 branches som fortsatt lå umerget (etter at
shallow-clone-historikken først ble fikset — se metodenotatet i selve
kartleggingen), delt i tre bøtter: branches som er ferdig kode og bare venter
på en merge-beslutning, NEEDS-JUDGMENT-punkter uten kode ennå, og
kreative idéer fra `idea-backlog.md`. Fredrik godkjente bøtte 1 direkte og ba
meg vurdere og velge vinner i to duplikat-par i bøtte 2; bøtte 3
(produktvalg-punktene) venter fortsatt bevisst på ham, se anbefalingslisten
under.

**Live i produksjon nå (etter push), utover det som står i tabellen over:**

| # | Kategori | Endring | Kilde-branch |
|---|----------|---------|--------------|
| 1 | 🔵 Backoffice | Pinch-zoom skrudd av kun på Mapbox-kartsider (`/`, `/sykle`, `/backoffice/tetthet`) i stedet for globalt, så pinch styrer kartets zoom i stedet for siden | `nightly/2026-07-13/scoped-viewport-zoom` |
| 2 | 🔵 Backoffice | Hurtigsvar-chips (5 faste norske fraser) på den offentlige saksoppdateringen i `/backoffice/sak/[id]` | Håndportert fra `nightly/2026-07-16/hurtigsvar-maler` (branchen var 5 uker gammel og bygget mot en siden slettet komponent — funksjonen ble hentet ut og satt inn i dagens saksside i stedet for å merge branchen rått) |
| 3 | 🔵 Backoffice | Nytt internt `/backoffice/statistikk`: meldingsvolum over tid, kategori-/status-fordeling, løsningstid (median/snitt), veieier-/vegkategori-fordeling fra NVDB, CSV-eksport uten kontaktinfo | Håndportert fra `nightly/2026-07-17/staff-stats-dashboard` — vurdert av en underagent mot konkurrenten `nightly/2026-07-17/backoffice-statistikk` og valgt som vinner (formålsbygd, begrenset datahenting fremfor å gjenbruke den tunge offentlige GeoJSON-henteren; mer operasjonelt nyttige tall) |
| 4 | 🟡 Borgervendt (utenfor nattens vanlige omfang, men bygget på direkte forespørsel fra Fredrik) | Ny `/tryggeste-skolevei`: velg start-/målpunkt på kartet, få 2–3 rutealternativer fra Mapbox Directions rangert Lav/Middels/Høy risiko etter nærhet til meldte farer (samme vekting som det offentlige Bekymringsgrad-varmekartet) og NVDB-ulykker langs ruten. Leser aldri/lagrer rå GPS-turdata, ingen ny hemmelighet | Håndportert fra `nightly/2026-07-17/safest-school-route` — vurdert av en underagent mot to konkurrerende forsøk (`skolevei-sjekk`, `combined-preview`) og valgt som vinner (gjenbruker eksisterende kartkomponent/vekting i stedet for å duplisere, dokumentert, degraderer gradvis ved NVDB-treghet) |

Punkt 2–4 kunne ikke merges direkte — branchene var 4–5 uker gamle, og et
råt `git merge` ville revertert store deler av arbeidet gjort på `main`
siden da (hundrevis av filer i diff, hele sider som er bygget senere ville
blitt slettet). Selve funksjonen i hver branch ble derfor hentet ut manuelt
og satt inn i dagens kodebase, build-verifisert grønt før hver commit, akkurat
som den vanlige nattlige prosessen — bare uten en egen isolert branch per
endring siden dette var en direkte, live samtale i stedet for den automatiske
rutinen.

**Droppet, ikke merget:**

- `nightly/2026-07-17/mine-tildelte-saker` — overflødig. `main` har siden fått
  en mer moden versjon av samme idé (dashboardets «Mine saker»-seksjon +
  «Uten ansvarlig»-filter + tildel-dropdown på sakssiden), bygget etter at
  denne branchen ble laget.
- `nightly/2026-07-13/ui-polish-batch-6` — for gammel til å trekke ut trygt.
  Diffen mot dagens `main` berører 131 filer og ville slettet hele sider
  (`/mine-turer`, `/personvern`, `/backoffice/skilt/[id]` m.fl.) som er bygget
  etter denne branchen. Innholdet (død CSS, a11y-opprydding) er trolig uansett
  gjort irrelevant av den store style-oppryddingen som har skjedd siden
  07-13.
- To andre skolevei-forsøk (`nightly/2026-07-16/skolevei-sjekk`,
  `nightly/2026-07-16/combined-preview`) og statistikk-duplikatet
  (`nightly/2026-07-17/backoffice-statistikk`) — tapte den direkte
  sammenligningen mot vinnerne over, ikke bygget.

`npm ci && npm run build` verifisert helt rent (fjernet `node_modules`/`.next`
først) etter siste endring i denne økten, før push.

## Funnet, men bevisst ikke bygget i natt

Ingen nye NEEDS-JUDGMENT-funn i natt.

## Anbefaling

Uendret, videreført fra 08-15 (ingen nye punkter i natt):

0. **Ta stilling til GPS-hjem-personvern-tillitsproblemet** (flagget
   2026-08-10, fortsatt viktigst) — to endepunkter (`/api/bike-trips`,
   `/api/report`) stoler på klient-oppgitt punktrekkefølge for en
   barnerelevant personvernbeskyttelse.
1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, bekreftet fortsatt uendret i natt) — appens mest trafikkerte
   endepunkt henter hele sakstabellen ubegrenset; en fiks som skjuler gamle
   saker er en synlig produktbeslutning, ikke mekanisk.
2. **Manglende Content-Security-Policy** (flagget 2026-08-06, bevisst utsatt
   fordi Mapbox GL/next-font/styled-jsx trenger nøye allowlisting — fortsatt
   ikke løst). NEEDS-JUDGMENT, ikke en mekanisk enlinjer — feil CSP kan
   stille bryte kartet/fonter.
3. **Ta stilling til AI-forslag-prompt-herding** (flagget 08-09) — heng den
   sammen med punkt 5 under når du vurderer AI-forslag-branchen.
4. **Ta stilling til retur-tur-personvernspørsmålet** (flagget 08-08) —
   henger sammen med punkt 0.
5. **Ta stilling til AI-forslag-UI-branchen**
   (`nightly/2026-08-02/backoffice-ai-suggestions-ui`, bygget for femten
   netter siden) — merge, be om endringer, eller la den ligge. Sekstende
   natten på rad den står urørt.
6. **Ta stilling til `listHotCases`-rangeringsskjevheten** og
   **konkurranse-redigering-UI** (begge flagget 08-10), samt
   **bruker-redigering-UI** og **seed-spor-nav-synlighet** (begge flagget
   08-11) — alle fire sover fortsatt, ingen hastverk.
7. **Ta stilling til intern-vedlegg-lagringsgapet** (flagget 2026-08-07) —
   `case_attachments` med `visibility: 'internal'` lagres i samme
   *offentlige* Supabase-bucket som offentlige vedlegg, uten objekt-nivå
   tilgangskontroll. Krever migrering til privat bucket + signerte URL-er.
8. **Vurder å rydde den ene foreldreløse `case_attachments`-raden i
   produksjon** (identifisert 2026-08-06) — destruktiv sletting, gjøres
   bevisst ikke automatisk.
9. **Ta stilling til Trello-kortløs-sak-begrensningen** (flagget
   2026-08-06).
10. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
    produksjon** (flagget hhv. 2026-08-02 og 2026-08-03) — uten dem kan
    forfalskede Trello-webhook-kall/cron-kall slippe gjennom.
11. Next.js-oppgraderingen (2 gjenstående høye `npm audit`-sårbarheter,
    krever en overvåket Next 14→16-major) står fortsatt uløst.
12. Dokumentasjonshullet fra 2026-08-05 (`staff`, `staff_sessions`,
    `case_attachments` mangler `CREATE TABLE`-avsnitt i README) er fortsatt
    ikke fikset — ren dokumentasjon, ingen hastverk.
13. Valgfritt opprydningspunkt, ingen hast: de to gamle, utdaterte
    bildekomprimering-forsøkene på
    `nightly/2026-07-13/image-compression-before-upload` og
    `nightly/2026-07-16/bildekomprimering` er fortsatt overflødige siden
    2026-08-04s `report-image-compression` dekker det samme.
