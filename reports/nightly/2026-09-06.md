# Nattlig gjennomgang & bygg — 2026-09-06

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som hver natt siden 2026-08-02: den lagrede trigger-teksten
ber om en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner»,
bygg minst 3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview,
aldri `main`). `CLAUDE.md` (sist oppdatert 2026-07-18, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt, som hver natt siden 08-02. Trigger-teksten er fortsatt ikke
oppdatert til å matche — rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt. Ingen nye
produktforslag dukket opp under gjennomgangen som ikke allerede står i
`idea-backlog.md` — den er derfor ikke endret i natt.

## Sammendrag

Baseline (`main`, commit `26b4b45`) verifisert grønn før noe ble bygget: ren
`npm ci` + `npm run build` uten feil, 0 sårbarheter.

Tre parallelle underagenter (sikkerhet, ytelse, backoffice) gjorde hver sin
uavhengige, dype gjennomgang av hele det aktuelle området, orientert om
rapportene fra 2026-09-04 og 2026-09-05 for å unngå duplikater. Ingen kode
hadde endret seg siden 09-05-mergen (HEAD var nøyaktig den mergen), så det
var ingen ny diff å regresjonssjekke utover det som ble sjekket i natt.

**Sikkerhet: ingen nye funn (36. natt på rad).** Gjennomgikk på nytt hele
`pages/api/**/*.js`-treet og `lib/*.js`, med spesielt fokus på ting ikke
enkeltvis navngitt i 09-04/09-05: `weather.js`/`aktuelt.js`/`lib/feeds.js`
(RSS/ICS-parsing), `nvdb/layer.js`s bbox-tak, hele `bike-trips.js`s
valideringssti + `lib/geoPrivacy.js` i sin helhet (anti-gjenskapbar
hjemsted-design holder), hele `lib/supabaseRest.js` linje for linje
(ingen PostgREST-injeksjon), `staffAuth.js`/`backofficeAuth.js`/
`timingSafeEqualStrings.js`, Trello-webhookens HMAC-verifisering, samt
hvert `dangerouslySetInnerHTML`-treffpunkt i kodebasen (alle går gjennom
en konsekvent escape-hjelper eller fast server-tekst) og
`lib/multipart.js`/`lib/fileSignature.js` (magic-byte-verifisering dekker
alle tillatte typer). `npm audit`: 0 sårbarheter.

**Ytelse: 1 SAFE-MECHANICAL-funn (bygget og merget).** Gjennomgikk
`getCompetitionStats`, `getPublicReportsByIds`, `getReportsSince`,
`getCaseSupport`, `nvdb/layer.js`, `backoffice/hotspots.js`,
`backoffice/competition-trips.js`, `lib/hotspotAnalysis.js`, samt
klientsidene `ReportMap.js`/`mine-meldinger.js`/`mine-turer.js`/
`backoffice/liste.js` på nytt — alt allerede begrenset/cachet fra
tidligere netter.

1. **[SAFE-MECHANICAL, bygget]** `listCaseAttachments()`
   (`lib/supabaseRest.js`) var den tredje parallelle spørringen i
   `getReportThread()` som fortsatt manglet et `limit=`, mens de to
   søsken-spørringene i nøyaktig samme `Promise.all` fikk taket sitt for
   flere netter siden: `report_supports` (`REPORT_THREAD_SUPPORT_LIMIT`,
   09-03) og `report_status_updates` (`REPORT_THREAD_UPDATES_LIMIT`,
   09-04). Det finnes ikke noe tak på antall vedlegg en sak kan samle opp
   over tid (`pages/api/backoffice/attachment.js` sjekker aldri
   eksisterende antall før innsetting), og spørringen treffes av tre
   steder: den offentlige, uautentiserte `GET /api/report-thread`, den
   offentlige `pages/sak/[id].js` (SSR, ingen cache), og den autentiserte
   backoffice-sakoppslaget. Fikset ved å legge til `CASE_ATTACHMENTS_LIMIT`
   (500, samme mønster som de to søsknene), hente nyest-først med taket og
   reversere til kronologisk rekkefølge før retur — mekanisk, ingen
   oppførselsendring for noen sak under taket.

**Backoffice: 1 SAFE-MECHANICAL-funn (bygget og merget).** Bekreftet at
kappløpsvakt-utrullingen i `sak/[id].js` (fra 09-03 til 09-05) nå dekker
alle åtte state-muterende handlere uten unntak, og at ingen søsterfil i
`pages/backoffice/**` har det samme mønsteret uten vakt. Gjennomgikk hele
`pages/backoffice/**` og `pages/api/backoffice/**` fresh likevel.

1. **[SAFE-MECHANICAL, bygget]** `flash` (feil-/suksess-banneret) og
   `referralCopied` ("Kopiert!"-etiketten på henvisningsknappen) i
   `pages/backoffice/sak/[id].js` var de to siste bitene sak-scopet
   UI-state som ikke ble nullstilt i den samme per-sak reset-effekten
   (linje 142–160) som allerede nullstiller `deleteArmed`/`note`/
   `attBusyId` osv. av nøyaktig samme grunn. Konkret scenario: en stab
   legger til et internt notat eller får en feilmelding på sak A ("Kunne
   ikke slette vedlegg"), navigerer umiddelbart videre til sak B med
   piltast — sak B viser da sak A-ets gamle banner, som kan feiltolkes
   som at noe nettopp skjedde på (eller er galt med) saken staben nå ser
   på. Samme for "Kopiert!"-etiketten hvis en stab kopierer
   henvisningsteksten og navigerer innen 2-sekundersvinduet. Fikset ved å
   legge `setFlash('')`/`setReferralCopied(false)` inn i den eksisterende
   reset-effekten — nøyaktig samme mønster som resten av filen.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for disse — de er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🟢 Ytelse | Cap `listCaseAttachments()`-spørringen i `getReportThread()` — siste av tre søsken-spørringer uten tak | `nightly/2026-09-06/case-attachments-cap` |
| 2 | 🔵 Backoffice | Nullstill `flash`/`referralCopied` i sak-navigasjonens reset-effekt | `nightly/2026-09-06/sak-flash-reset` |

Hver branch ble bygget og build-verifisert (`npm ci` + `npm run build`)
enkeltvis først, deretter merget inn i `main` (to rene merger, ingen
konflikt). Kombinert `main` ble bygget grønt på nytt (`npm ci && npm run
build`, helt ren `node_modules`/`.next`, ingen cache) rett før push.
`npm audit`: 0 sårbarheter, både før og etter. `main` er nå på `fbfcf47`.

## Ingenting bygget kun som preview i natt

Ingen nye NEEDS-JUDGMENT-funn i natt. Begge funnene (ytelse og backoffice)
var entydig SAFE-MECHANICAL og er allerede merget.

## Sjekk dette (for de to som er live)

1. **`listCaseAttachments()`-taket**: åpne en sak med vedlegg i backoffice
   (`/backoffice/sak/[id]`) og på den offentlige sak-siden (`/sak/[id]`),
   bekreft at vedleggene fortsatt vises i riktig kronologisk rekkefølge
   (eldst til nyest) som før. Ingen synlig endring forventet for noen sak
   med færre enn 500 vedlegg (alle saker i dag).
2. **`flash`/`referralCopied`-nullstillingen**: legg til et internt notat
   eller trigg en feilmelding (f.eks. slett et vedlegg) på en sak, naviger
   umiddelbart videre til neste sak med piltast, og bekreft at det nye
   bannerert ikke lenger vises på den nye saken. Tilsvarende: klikk "Kopier
   tekst" på henvisningskortet, naviger raskt videre, og bekreft at "Kopiert!"
   ikke henger igjen på den nye saken.

## Anbefaling

Videreført fra 09-05, uendret (ingen av de 15 punktene ble adressert i
natt — kveldens to funn var nye og er allerede løst og fjernet fra
listen):

1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, fortsatt uendret).
2. **Avgjør `waitUntil`-branchen fra 08-25**
   (`nightly/2026-08-25/report-email-waituntil`) — bygget og
   build-verifisert, venter fortsatt på din vurdering i preview før
   eventuell merge.
3. `listHotCases`-rangeringsskjevheten, konkurranse-redigering-UI,
   bruker-redigering-UI, seed-spor-nav-synlighet — alle sover fortsatt.
4. **Rydd den ene foreldreløse `case_attachments`-raden** — destruktiv,
   bevisst ikke automatisk.
5. **Trello-kortløs-sak-begrensningen** — fortsatt åpent, venter på deg.
6. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
   produksjon** — kan ikke bekreftes fra denne økten. Fortsatt det
   lengst-stående uavklarte punktet på listen (siden 08-02, nå over en
   måned).
7. Valgfritt opprydningspunkt: de to gamle bildekomprimering-branchene er
   fortsatt overflødige.
8. **FK-constraint `case_attachments.report_id REFERENCES reports(id) ON
   DELETE CASCADE`** — fortsatt åpent, additiv migrasjon, ingen hastverk.
9. **Cache signerte vedlegg-URL-er** — fortsatt NEEDS-JUDGMENT, ingen hast.
   (Kveldens `listCaseAttachments()`-tak reduserer ikke dette punktet —
   hver internal-rad signeres fortsatt individuelt på hvert lesetilfelle,
   bare med et øvre tak på antall rader nå.)
10. **Konsolider `distMeters`/haversine-duplikater** — fortsatt
    NEEDS-JUDGMENT.
11. `cells`-array-lengdetak i `POST /api/bike-trips` — bekreftet 08-25 at
    det ikke er et reelt problem, kan lukkes permanent med mindre du vil ha
    det av ren symmetri med `path`.
12. Signert-URL-lekkasje-monitor for interne vedlegg (rent produktforslag
    fra 08-28, se `idea-backlog.md`) — kun aktuelt om/når omfanget utvides
    tilbake til produktideer.
13. **Backoffice-UI for de eksisterende AI-forslag-endepunktene**
    (`/api/backoffice/ai/*`) — fortsatt uten UI som kaller dem, fortsatt et
    reelt produktvalg — NEEDS-JUDGMENT, ikke bygget.
14. **Trello-webhook uten replay-beskyttelse** (flagget 09-03) — lav
    praktisk alvorlighet (idempotent/spam-nivå, krever allerede en gyldig
    signert payload), men krever et lagrings-/TTL-designvalg for dedup på
    `action.id` før den kan bygges.
15. **Batchede support/status-spørringer uten per-sak-cap** i
    `getPublicReportGeoJson`/`listHotCases` (flagget 09-04) — lav/
    spekulativ alvorlighet i dag, men krever et spørrings-form- eller
    cap-valg, ikke en mekanisk patch.

36 netter uten nye sikkerhetsfunn av betydning, og ytelses-/backoffice-
vinduet er nå tynnere enn noensinne: begge kveldens funn var den aller
siste gjenværende instansen av et mønster to tidligere netter allerede
hadde ryddet opp i de fleste tilfellene av (siste søsken-spørring uten
tak; siste to biter UI-state uten reset). Det er ikke lenger noe tydelig
"neste" mønster å lete etter i disse to filene/funksjonene spesifikt —
gjenstående funn vil trolig kreve å lete bredere i kodebasen fremover.
Vurderingen fra i går er uendret og forsterkes: verdt å vurdere om det er
tid for enten (a) et bevisst signal fra deg om å utvide omfanget tilbake
til produktideer, eller (b) en gjennomgang av de 15 åpne punktene for å
avgjøre noen av dem — særlig punkt 6, som fortsatt er det eneste punktet
direkte knyttet til produksjons-sikkerhetskonfigurasjon jeg ikke kan
bekrefte selv, og som nå har stått uavklart i over en måned.
