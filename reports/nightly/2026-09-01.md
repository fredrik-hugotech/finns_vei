# Nattlig gjennomgang & bygg — 2026-09-01

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som hver natt siden 2026-08-02: den lagrede trigger-teksten
ber om en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner»,
bygg minst 3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview,
aldri `main`). `CLAUDE.md` (sist oppdatert 2026-07-18, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt, som hver natt siden 08-02. Trigger-teksten er fortsatt ikke
oppdatert til å matche — rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt. Ingen nye
produktforslag dukket opp under gjennomgangen som ikke allerede står i
`idea-backlog.md` — den er derfor ikke endret i natt.

## Sammendrag

Baseline (`main`, commit `d669f54`) verifisert grønn før noe ble bygget: ren
`npm ci` + `npm run build` uten feil, helt ren `node_modules`/`.next`, 0
sårbarheter.

Tre parallelle underagenter (sikkerhet, ytelse, backoffice) gjorde hver sin
uavhengige, dype gjennomgang av hele det aktuelle området, orientert om
rapportene fra 2026-08-29 til 2026-08-31 for å unngå duplikater.

**Sikkerhet: 31. natt på rad uten nye funn.** Fersk, adversarial gjennomgang
av surfaces ikke eksplisitt navngitt i de tre siste rapportene:
konkurransemodulen ende-til-ende (`pages/api/bike-trips.js`,
`pages/api/competitions/**`, `pages/api/backoffice/competitions.js`,
`pages/api/backoffice/competition-trips.js` — plausibilitetsgrenser,
admin-gating, klubb-allowliste), `GET /api/my-reports-status` (ID-er
enkeltvis `encodeURIComponent`-escapet før PostgREST `in.()`-filter, ingen
injeksjon), OG-bilde-rutene (Satori-baserte, ingen
`dangerouslySetInnerHTML`, all tekst auto-escapet via React children),
`GET/POST /api/cron/daily-summary` og den daglige e-postoppsummeringen
(tidssikker `CRON_SECRET`-sjekk, alle feltverdier i HTML-e-posten går
gjennom `esc()`, ingen HTML-injeksjon-til-stab-e-post-vei funnet), hele
stab-kontostyringen (`staff/bootstrap.js`, `users.js`, `password.js`,
`list.js`, `login.js` — rollesjekk, selvdeaktivering-/
siste-superbruker-sperrer, tidssikker dummy-hash-innlogging mot
konto-enumerering, `password_hash` aldri eksponert), og
bildeopplastings-validering (`lib/fileSignature.js` — magic-byte-verifisering,
filnavn-sanering mot path traversal). `npm audit`: 0 sårbarheter. Ingen nye
produktforslag.

**Ytelse (2 funn), SAFE-MECHANICAL, bygget og merget:**

1. `lib/reportWorkflow.js` sin `enrichReportBestEffort` kjørte
   `updateTrelloCardDescription()` og `attachImagesToTrelloBestEffort()`
   sekvensielt selv om dette er to uavhengige Trello REST-kall (PUT-
   beskrivelse vs. POST-vedlegg) uten datauavhengighet mellom seg, begge
   allerede feilhåndtert/best-effort innenfor samme `try/catch`.
   Parallellisert med `Promise.all` — samme mønster som gjentatte ganger
   tidligere denne uken. Treffer hver eneste allerede Trello-lenkede melding
   som fullfører NVDB-berikelse, altså de fleste meldingene, det meste av
   tiden.
2. `lib/reportWorkflow.js` sin `tryGroupIntoExistingCase` hadde samme
   mønster: `addTrelloCardComment()` og `attachImagesToTrelloBestEffort()`
   kjørte sekvensielt på to uavhengige Trello-endepunkter (POST-kommentar
   vs. POST-vedlegg), samme feilhåndteringsmønster. Parallellisert likt.
   Treffer alle sak-grupperte meldinger (ny melding innenfor
   `CASE_GROUP_RADIUS_M` av en åpen sak).

Begge er samme feilklasse som er fikset flere ganger i `reportWorkflow.js`
denne uken (08-31, 08-27/08-28) — to nye forekomster i samme fil som ikke
ble fanget opp i 08-31-gjennomgangen av filen.

**Backoffice (1 funn), SAFE-MECHANICAL, bygget og merget:**

1. `pages/backoffice/sak/[id].js` sin lim-inn-vedlegg-lytter
   (`useEffect` for `window.addEventListener('paste', ...)`, linje
   ~412–422) har avhengighetsarray `[id, uploadVis]`, så `onPaste`-
   lukningen — og dermed `doUpload`-referansen den kaller — blir stående
   med hvilken som helst `uploading`-verdi som var gjeldende sist effekten
   kjørte på nytt, ikke gjeldende state. Dermed gjaldt ikke
   08-29-fiksens `uploading`-busy-vakt for lim-inn-stien i praksis, kun for
   drag-drop og filvelgeren (inline JSX-handlere, ferske hver render). En
   stab som limer inn flere skjermbilder raskt etter hverandre (f.eks.
   videresendte e-posttråder som skjermbilder — en realistisk arbeidsflyt)
   kunne dermed starte overlappende opplastingsbatcher via lim-inn-stien.
   Fikset med en `uploadingRef` som muteres direkte i stedet for å lese den
   lukkede `uploading`-staten — samme mønster som `requestIdRef` lenger opp
   i samme fil — slik at alle forbrukere av `doUpload`, uansett når deres
   lukning ble fanget, leser samme delte referanse. Ingen produktvalg
   involvert.

Alle tre bygget på isolerte `nightly/2026-09-01/<slug>`-brancher,
build-verifisert individuelt, og merget.

Ingen NEEDS-JUDGMENT-funn i natt — alle tre funn var trygge, mekaniske,
tydelig i omfang, uten produktvalg eller ny hemmelighet.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for disse — de er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🟢 Ytelse | Parallelliser Trello-beskrivelse-/bildeoppdatering i `enrichReportBestEffort` | `nightly/2026-09-01/report-workflow-parallel-enrich-trello` |
| 2 | 🟢 Ytelse | Parallelliser Trello-kommentar-/bildeoppdatering i `tryGroupIntoExistingCase` | `nightly/2026-09-01/report-workflow-parallel-comment-attach` |
| 3 | 🔵 Backoffice | Fiks stale-closure-busy-vakt i sakens lim-inn-vedlegg-lytter | `nightly/2026-09-01/sak-paste-upload-guard-ref` |

Alle tre branchene ble bygget og build-verifisert (`npm ci` + `npm run
build`) enkeltvis først, deretter merget inn i `main` én etter én (rene
merges, ingen konflikter). Kombinert `main` bygget grønt på nytt (`npm ci &&
npm run build`, helt ren `node_modules`/`.next`, ingen cache) rett før push.
`npm audit`: 0 sårbarheter, både før og etter. `main` er nå på `07b254b`.

## Ingenting bygget kun som preview i natt

Kveldens tre funn krevde ikke noe produktvalg, ny hemmelighet eller ny
kjøretidsavhengighet — trygge nok til direkte merge under gjeldende
fullmakt.

## Anbefaling

Videreført fra 08-31 (uendret — ingenting på denne listen ble avklart eller
løst i natt):

1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, fortsatt uendret).
2. **Avgjør `waitUntil`-branchen fra 08-25**
   (`nightly/2026-08-25/report-email-waituntil`) — bygget og
   build-verifisert, venter fortsatt på din vurdering i preview før
   eventuell merge (ny kjøretidsavhengighet, se 08-25-rapporten for
   test-steg).
3. `listHotCases`-rangeringsskjevheten, konkurranse-redigering-UI,
   bruker-redigering-UI, seed-spor-nav-synlighet — alle sover fortsatt.
4. **Rydd den ene foreldreløse `case_attachments`-raden** — destruktiv,
   bevisst ikke automatisk.
5. **Trello-kortløs-sak-begrensningen** — fortsatt åpent, venter på deg.
6. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
   produksjon** — kan ikke bekreftes fra denne økten.
7. Valgfritt opprydningspunkt: de to gamle bildekomprimering-branchene er
   fortsatt overflødige.
8. **FK-constraint `case_attachments.report_id REFERENCES reports(id) ON
   DELETE CASCADE`** — fortsatt åpent, additiv migrasjon, ingen hastverk.
9. **Cache signerte vedlegg-URL-er** — fortsatt NEEDS-JUDGMENT, ingen hast.
10. **Konsolider `distMeters`/haversine-duplikater** — fortsatt
    NEEDS-JUDGMENT (ulike signaturer/kallformer er et reelt valg).
11. `cells`-array-lengdetak i `POST /api/bike-trips` — bekreftet 08-25 at
    det ikke er et reelt problem, kan lukkes permanent med mindre du vil ha
    det av ren symmetri med `path`.
12. Signert-URL-lekkasje-monitor for interne vedlegg (rent produktforslag
    fra 08-28, se `idea-backlog.md`) — kun aktuelt om/når omfanget utvides
    tilbake til produktideer.
13. **Backoffice-UI for de eksisterende AI-forslag-endepunktene**
    (`/api/backoffice/ai/*`) — fortsatt uten UI som kaller dem, fortsatt et
    reelt produktvalg (skal AI-forslag vises i det hele tatt igjen, jf.
    reverten 18.08) — NEEDS-JUDGMENT, ikke bygget.

Ingen nye punkter lagt til i natt — dette er samme liste som 08-31, kun
videreført. 31 netter uten sikkerhetsfunn og et jevnt tynnere strømmet
ytelses-/backoffice-vindu (2 + 1 små, mekaniske funn i natt, likt
08-31-nivået) tyder på at flaten nå er svært godt herdet innenfor det
gjeldende omfanget; det som gjenstår er i hovedsak reelle produktvalg
(skalering, cachingavveininger, opprydning, AI-forslag-UI) fremfor
uoppdagede feil. Verdt å vurdere om det er tid for enten (a) et bevisst
signal fra deg om å utvide omfanget tilbake til produktideer (jf.
`idea-backlog.md`, som har flere klare, avklarte forslag liggende), eller
(b) en gjennomgang av listen over 13 åpne punkter over for å avgjøre noen av
dem, siden gjennomgangs-/byggerutinen i sin nåværende form nærmer seg et
metningspunkt der «ingen nye funn» blir den vanlige natten.
