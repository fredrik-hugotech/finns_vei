# Nattlig gjennomgang & bygg — 2026-08-14

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Fjortende natt på rad med samme situasjon: den lagrede trigger-teksten ber om en
bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner», bygg minst
3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview, aldri
`main`). `CLAUDE.md` (sist oppdatert 2026-07-18 av deg, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Pause-vinduet (til og med
2026-08-01) er for lengst omme. Det er `CLAUDE.md` som er fulgt i natt, samme
som de tretten foregående nettene. Trigger-teksten er fortsatt ikke oppdatert
til å matche — anbefalingen står fra før: rett den opp om dette ikke er
meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt. `idea-backlog.md`
er uendret siden sist — ingenting nytt utenfor de tre tillatte kategoriene
dukket naturlig opp under gjennomgangen i natt.

## Sammendrag

Baseline (`main`, commit `8a9f14b`) verifisert grønn: ren `npm ci` + `npm run
build` uten feil. `npm audit`: 2 høye sårbarheter (samme kjente
next/postcss-par som forrige natt, ingen nye).

Tre parallelle underagenter (sikkerhet, ytelse, backoffice), hver orientert
om de fem siste netters funn for å unngå duplikater, gjorde en dyp
gjennomgang:

- **Sikkerhet**: 1 nytt funn (SAFE-MECHANICAL), bygget. Gjennomgikk på nytt
  alle `pages/api/**`-ruter, auth/sesjons-/rate-limit-hjelpere,
  filopplasting, XSS-flater, PostgREST-spørringsbygging, samt diffene til de
  5 siste nattlige merges — ingen reverterte eller ødelagte tidligere fikser.
- **Ytelse**: 1 nytt funn (SAFE-MECHANICAL), bygget. Resten av kodebasen
  (cron/daily-summary, aktuelt-feeden, konkurranse-sertifikat-ruten,
  mine-meldinger/mine-turer, backoffice-dashbord/liste, saksarbeidsflatens
  effekter) bekreftet fortsatt korrekt fra tidligere netters fikser.
- **Backoffice**: 3 nye funn (alle SAFE-MECHANICAL), bygget.

5 mekaniske fikser er bygget hver på egen branch, build-verifisert grønt,
**merget til `main`** (5 rene merges, ingen konflikter), og `main` er
build-verifisert grønt på nytt (helt ren `npm ci` + `.next`, ingen
node_modules-cache) etter siste merge, før push.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for disse — de er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon. Listet i den
rekkefølgen de ble merget.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🔒 Sikkerhet | `authorized()` i `pages/api/cron/daily-summary.js` var siste gjenværende `=== secret`-sammenligning i `pages/api/**`/`lib/*.js` (bekreftet via full gjennomgang) — en timing-side-kanal på `CRON_SECRET`, ulikt alle andre hemmelighet-gatede endepunkter (`isBackofficeAuthorized`, `isDebugAuthorized`) som allerede bruker `timingSafeEqualStrings`. Byttet begge sammenligninger (Bearer-header og `?key=`-fallback) til `timingSafeEqualStrings` | `nightly/2026-08-14/cron-timing-safe-secret` |
| 2 | 🟢 Ytelse | `getPublicReportById()` hentet hele `reports`-raden (`select=*`, ~35 kolonner inkl. flere jsonb-felt) kun for å plukke ut/kaste tre kontaktfelt — samme oversized-payload-mønster fikset andre steder 08-11/08-13, men denne kallstedet (brukt av både `/sak/[id]` og dens OG-bilde-søsken) sto igjen. Byttet til et eksplisitt minimalt select (`id,category,description,status,image_urls,public_status_note`); kontaktfelt-strippingen er da overflødig og fjernet | `nightly/2026-08-14/public-report-minimal-select` |
| 3 | 🔵 Backoffice | Dashbordets `useEffect` for `/api/backoffice/cases` hadde ingen feilhåndtering: ethvert ikke-200-svar (utløpt økt → 403, rate-limit → 429, serverfeil → 500) ga en stille `null`, og siden ble stående på «Laster …» for alltid uten feilmelding eller vei tilbake til innlogging — ulikt `liste.js`/`hotteste.js`/`gjentakende-steder.js`, som alle allerede håndterer dette med `describeFetchError` + eksplisitt «Logg inn først»-melding på 403. Speiler nå samme mønster, inkl. «Logg inn på nytt»-lenke ved utløpt økt | `nightly/2026-08-14/dashboard-fetch-error` |
| 4 | 🟢 Ytelse | `/backoffice/skilt/[id].js` (printbar QR-skilt-side) hentet også hele `reports`-raden via `getReportById(id)` uten select-override, selv om siden kun leser `id`/`category`/`description`/`status` — samme mønster fikset ved `cases.js`/`attachment.js` 08-13, men dette kallstedet ble oversett den natten. Lagt til eksplisitt minimalt select | `nightly/2026-08-14/skilt-minimal-select` |
| 5 | 🔵 Backoffice | POST-grenen i `pages/api/backoffice/competitions.js` validerer at navn ikke er tomt og at til-dato ikke er før fra-dato (08-12/08-13), men PATCH-grenen la `req.body` rett på `updateCompetition()` uten noen sjekk — samme feilklasse, nåbar via et direkte API-kall (f.eks. `BACKOFFICE_SECRET` + curl) med en annen HTTP-verb. Speiler nå de samme to sjekkene fra POST når feltene er med i PATCH-bodyen | `nightly/2026-08-14/competition-patch-validation` |

Kombinert `main` bygget grønt (`npm ci && npm run build` med helt ren
`node_modules`/`.next`, ingen cache) rett før push, etter merge nr. 5.
`npm audit` uendret på 2 høye sårbarheter (next/postcss, se punkt 11 i
anbefalingslisten under).

## Funnet, men bevisst ikke bygget i natt

Ingenting nytt i denne kategorien i natt — alle fem funn var trygge nok til
å bygges og merges direkte. Ingen av underagentene fant noe nytt
NEEDS-JUDGMENT-funn eller noen ny kreativ/borgervendt idé denne natten.

## Anbefaling

Samlet, prioritert oppfølgingsliste (mest videreført fra tidligere netter,
uendret siden 08-13 siden ingenting nytt kom opp i natt):

0. **Ta stilling til GPS-hjem-personvern-tillitsproblemet** (flagget
   2026-08-10, fortsatt viktigst) — to endepunkter (`/api/bike-trips`,
   `/api/report`) stoler på klient-oppgitt punktrekkefølge for en
   barnerelevant personvernbeskyttelse.
1. **Manglende Content-Security-Policy** (flagget 2026-08-06, bevisst
   utsatt fordi Mapbox GL/next-font/styled-jsx trenger nøye allowlisting —
   fortsatt ikke løst). NEEDS-JUDGMENT, ikke en mekanisk enlinjer — feil CSP
   kan stille bryte kartet/fonter.
2. **Ta stilling til AI-forslag-prompt-herding** (flagget 08-09) — heng den
   sammen med punkt 4 under når du vurderer AI-forslag-branchen, og den
   lille `'Forbidden'`→`'Ingen tilgang'`-teksfiksen i AI-endepunktene
   (flagget 08-12, fortsatt bevisst urørt av samme grunn).
3. **Ta stilling til retur-tur-personvernspørsmålet** (flagget 08-08) —
   henger sammen med punkt 0.
4. **Ta stilling til AI-forslag-UI-branchen**
   (`nightly/2026-08-02/backoffice-ai-suggestions-ui`, bygget for tolv
   netter siden) — merge, be om endringer, eller la den ligge. Trettende
   natten på rad den står urørt.
5. **Ta stilling til `listHotCases`-rangeringsskjevheten** og
   **konkurranse-redigering-UI** (begge flagget 08-10) — begge sover i
   dag, ingen hastverk.
6. **Ta stilling til bruker-redigering-UI** og **seed-spor-nav-synlighet**
   (begge flagget 08-11) — samme «ingen hastverk, men verdt en bevisst
   avgjørelse»-status som punkt 5.
7. **Ta stilling til intern-vedlegg-lagringsgapet** (flagget 2026-08-07) —
   `case_attachments` med `visibility: 'internal'` lagres i samme
   *offentlige* Supabase-bucket som offentlige vedlegg, uten objekt-nivå
   tilgangskontroll. Krever migrering til privat bucket + signerte URL-er.
8. **Vurder å rydde den ene foreldreløse `case_attachments`-raden i
   produksjon** (identifisert 2026-08-06) — destruktiv sletting, gjøres
   bevisst ikke automatisk.
9. **Ta stilling til Trello-kortløs-sak-begrensningen** (flagget
   2026-08-06).
10. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
    produksjon** (flagget hhv. 2026-08-02 og 2026-08-03) — uten dem kan
    forfalskede Trello-webhook-kall/cron-kall slippe gjennom.
11. Next.js-oppgraderingen (2 gjenstående høye `npm audit`-sårbarheter,
    krever en overvåket Next 14→16-major) står fortsatt uløst.
12. Dokumentasjonshullet fra 2026-08-05 (`staff`, `staff_sessions`,
    `case_attachments` mangler `CREATE TABLE`-avsnitt i README) er fortsatt
    ikke fikset — ren dokumentasjon, ingen hastverk.
13. Valgfritt opprydningspunkt, ingen hast: de to gamle, utdaterte
    bildekomprimering-forsøkene på
    `nightly/2026-07-13/image-compression-before-upload` og
    `nightly/2026-07-16/bildekomprimering` er fortsatt overflødige siden
    2026-08-04s `report-image-compression` dekker det samme.
