# Nattlig gjennomgang & bygg — 2026-08-30

## Om kveldens trigger-prompt vs. `CLAUDE.md`

Samme situasjon som hver natt siden 2026-08-02: den lagrede trigger-teksten
ber om en bred, kreativ runde («finn 5–10 kreative nye ideer/funksjoner»,
bygg minst 3–5 inkludert én ambisiøs ny borgervendt funksjon, kun preview,
aldri `main`). `CLAUDE.md` (sist oppdatert 2026-07-18, sier eksplisitt at
den overstyrer alt annet inkludert den automatiserte rutinen) har siden
2026-08-02 et innsnevret omfang — kun sikkerhet, ytelse og
backoffice-forbedringer — pluss stående fullmakt til å merge direkte til
`main`/produksjon for funn i de tre kategoriene. Det er `CLAUDE.md` som er
fulgt i natt, som hver natt siden 08-02. Trigger-teksten er fortsatt ikke
oppdatert til å matche — rett den opp om dette ikke er meningen.

Ingen kreative/nye borgervendte produktideer er bygget i natt. Ingen nye
produktforslag dukket opp under gjennomgangen som ikke allerede står i
`idea-backlog.md` — den er derfor ikke endret i natt.

## Sammendrag

Baseline (`main`, commit `1e74520`) verifisert grønn før noe ble bygget: ren
`npm ci` + `npm run build` uten feil, helt ren `node_modules`/`.next`, 0
sårbarheter.

Tre parallelle underagenter (sikkerhet, ytelse, backoffice) gjorde hver sin
uavhengige, dype gjennomgang av hele det aktuelle området (ikke bare nylige
endringer), orientert om gårsdagens (08-29) og nærmeste tidligere rapporter
for å unngå duplikater.

**Sikkerhet: 29. natt på rad uten nye funn.** En fersk adversarial
gjennomgang av overflater som ikke var eksplisitt navngitt i de siste
rapportene — QR-kodegenerering (`lib/qrEncode.js`, mater
`dangerouslySetInnerHTML` på skilt-siden, men bekreftet bygget kun fra det
numeriske modulrutenettet, ingen injeksjonsvei), mailto-henvisningsutkastet
(`lib/roadAuthorityReferral.js`, korrekt `encodeURIComponent`-innkapslet),
OG-bilderutene, vær-/aktuelt-proxyene (ingen bruker-kontrollert SSRF),
feed-parsing (ingen XXE), det offentlige `report-thread`-endepunktet (IDOR-
grense fortsatt intakt), vedleggsopplasting (filnavn-sanering mot
path traversal, innholdstype verifisert mot faktiske filbytes), samt
`staff/bootstrap` og cron-hemmelighetssjekken — alt bekreftet korrekt.
`npm audit`: 0 sårbarheter. Ingen nye produktforslag.

**Ytelse: ingen nye funn.** Full gjennomgang av `lib/supabaseRest.js`, alle
`pages/api/**`-ruter og de tyngste backoffice-sidene bekreftet at det
etablerte mønsteret (parallelle uavhengige I/O-kall, id-avgrensede oppslag,
eksplisitte radtak, 5-minutters cache på leseintensive admin-visninger,
minimale `select`-lister) allerede er konsekvent anvendt overalt. Ingen
manglende databaseindekser mot de faktiske spørringsformene. Ett mulig
tema (`getReportsSince()` uten eksplisitt radtak) ble bevisst *ikke* rapport-
ført som funn — datamengden gjør det praktisk irrelevant i dag, og en
kunstig innføring av en grense ville ikke matche noen etablert presedens.

**Backoffice (1 funn), SAFE-MECHANICAL, bygget og merget:**

1. Vedleggenes "Gjør offentlig/intern"- og "Slett"-knapper på saksarbeids-
   flaten (`pages/backoffice/sak/[id].js`) manglet samme busy-vakt som
   status-/frist-/ansvarlig-kontrollene og opplastingen på nøyaktig samme
   side allerede fikk i natt (08-29) — samme feilklasse, én instans til.
   Et dobbeltklikk på synlighets-vekslingen kunne sende to overlappende
   PATCH-kall som begge flytter samme fil mellom Storage-bucketer
   (`setCaseAttachmentVisibility` → `moveStorageObject`): det andre kallets
   nedlasting kan feile etter at det første allerede har slettet
   kildeobjektet, og staben ser en forvirrende feilmelding selv om
   togglingen egentlig lyktes. Fikset med samme mønster som resten av siden
   (ny `attBusyId`-state, guard i begge funksjonene, `disabled` på begge
   knappene) — ren mekanisk parallell til gårsdagens fiks, ingen
   atferdsendring utover å blokkere et dobbeltklikk.

1 fiks bygget i natt, SAFE-MECHANICAL, merget rett til `main`/produksjon.

## Allerede live i produksjon i natt (etter push)

Ingen handling nødvendig fra deg for denne — den er merget og deployes
automatisk til `finnsvei.no` via Vercels Git-integrasjon.

| # | Kategori | Endring | Branch |
|---|----------|---------|--------|
| 1 | 🔵 Backoffice | Vedleggenes toggle-/slett-knapper på saksflaten deaktiveres nå mens egen kall pågår, samme mønster som status/frist/ansvarlig/opplasting fikk i går | `nightly/2026-08-30/attachment-busy-guard` |

Branchen ble bygget og build-verifisert (`npm ci` + `npm run build`) først,
deretter merget inn i `main`. Kombinert `main` bygget grønt på nytt (`npm ci
&& npm run build`, helt ren `node_modules`/`.next`, ingen cache) rett før
push. `npm audit`: 0 sårbarheter, både før og etter.

## Ingenting bygget kun som preview i natt

Kveldens ene funn krevde ikke noe produktvalg, ny hemmelighet eller ny
kjøretidsavhengighet — trygt nok til direkte merge under gjeldende fullmakt.

## Anbefaling

Videreført fra 08-29 (uendret — ingenting på denne listen ble avklart eller
løst i natt):

1. **Ta stilling til `GET /api/reports`-skaleringsspørsmålet** (flagget
   2026-08-02, fortsatt uendret).
2. **Avgjør `waitUntil`-branchen fra 08-25**
   (`nightly/2026-08-25/report-email-waituntil`) — bygget og
   build-verifisert, venter fortsatt på din vurdering i preview før
   eventuell merge (ny kjøretidsavhengighet, se 08-25-rapporten for
   test-steg).
3. `listHotCases`-rangeringsskjevheten, konkurranse-redigering-UI,
   bruker-redigering-UI, seed-spor-nav-synlighet — alle sover fortsatt.
4. **Rydd den ene foreldreløse `case_attachments`-raden** — destruktiv,
   bevisst ikke automatisk.
5. **Trello-kortløs-sak-begrensningen** — fortsatt åpent, venter på deg.
6. **Bekreft om `TRELLO_API_SECRET` og `CRON_SECRET` er satt i
   produksjon** — kan ikke bekreftes fra denne økten.
7. Valgfritt opprydningspunkt: de to gamle bildekomprimering-branchene er
   fortsatt overflødige.
8. **FK-constraint `case_attachments.report_id REFERENCES reports(id) ON
   DELETE CASCADE`** — fortsatt åpent, additiv migrasjon, ingen hastverk.
9. **Cache signerte vedlegg-URL-er** — fortsatt NEEDS-JUDGMENT, ingen hast.
10. **Konsolider `distMeters`/haversine-duplikater** — fortsatt
    NEEDS-JUDGMENT (ulike signaturer/kallformer er et reelt valg).
11. `cells`-array-lengdetak i `POST /api/bike-trips` — bekreftet 08-25 at
    det ikke er et reelt problem, kan lukkes permanent med mindre du vil ha
    det av ren symmetri med `path`.
12. Signert-URL-lekkasje-monitor for interne vedlegg (rent produktforslag
    fra 08-28, se `idea-backlog.md`) — kun aktuelt om/når omfanget utvides
    tilbake til produktideer.

Enda en natt med et stadig tynnere funnvindu siden omfanget ble innsnevret
2026-08-02: sikkerhet og ytelse fant ingenting nytt (29. og et ukjent-men-
langt antall netter på rad uten funn), og backoffice fant nøyaktig én liten,
mekanisk instans av en feilklasse gårsdagen allerede hadde fikset det
meste av på samme side. Sikkerhets-/ytelses-/backoffice-flaten fremstår
fortsatt svært godt herdet; gjenstående åpne punkter er i hovedsak reelle
produktvalg (skalering, cachingavveininger, opprydning, en full
kode-konsolidering) fremfor uoppdagede feil.
